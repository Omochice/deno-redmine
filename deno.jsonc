{
  "tasks": {
    "fmt": "deno fmt ./**/*.ts",
    "fmt:check": "deno fmt --check ./**/*.ts",
    "check": "deno check ./**/*.ts",
    "lint": "deno lint ./**/*.ts",
    "test": "deno test --unstable-broadcast-channel",
    "test:coverage": "mkdir coverage -p && deno test --unstable-broadcast-channel --coverage=coverage/coverage && deno coverage coverage/coverage --lcov --output=coverage/lcov.info"
  },
  "lock": false,
  "fmt": {
    "exclude": ["CHANGELOG.md"]
  }
}
